switch (비교할값) {
  case 값1: {
     //값1 일때, 실행될 코드
    break; // switch문을 빠져나온다.
  }
  case 값2: {
     //값2 일때, 실행될 코드
    break; // switch문을 빠져나온다.
  }
  case 값3: {
     //값3 일때, 실행될 코드
    break; // switch문을 빠져나온다.
  }
  case 값4: {
     //값4 일때, 실행될 코드
    break; // switch문을 빠져나온다.
  }

  default: {
    // 비교할 값이 어떤 case도 일치하지 않을 때,
    // default 선택사항
  }
}

